This is a leraning project for the backend env, including all basic usage for nodejs.
In this project we will be using some librarys: 
    "bcrypt": "^5.1.1",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "cryptr": "^6.3.0",
    "express": "^4.21.1"
we will also be using this as a module type, import and export es6 (js) and not require.

functionality: 
going to index.html (frontend project) including the fallback url in case of unknown url to this project.
this will be focusing on the api and crud for bugs and users.

